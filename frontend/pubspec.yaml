name: frontend
description: NorthBridge frontend app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  http: ^1.2.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  firebase_data_connect: ^0.3.0
  speech_to_text: ^6.6.0
  geolocator: ^11.0.0
  google_maps_flutter: ^2.5.0
  file_picker: ^8.0.7
  web_socket_channel: 3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true